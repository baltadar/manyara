<section class="relative h-screen overflow-hidden text-white">
  <!-- Background Video -->
  <video
    id="bg-video"
    autoplay
    muted
    playsinline
    class="absolute top-0 left-0 w-full h-full object-cover z-0"
    style="filter: brightness(1.5) contrast(1.3) saturate(1.1);"
  >
    <source
      src="https://res.cloudinary.com/ddwam3xle/video/upload/v1745440346/Manyara2_c3dj38.mp4"
      type="video/mp4"
    />
    Your browser does not support the video tag.
  </video>

  <!-- Overlay (optional gradient/darkening) -->
  <div class="absolute inset-0 bg-gradient-to-br from-slate-900/60 to-blue-900/40 z-10"></div>

  <!-- Text Overlay -->
  <div
    class="absolute bottom-10 left-1/2 transform -translate-x-1/2 z-20 bg-black/40 px-12 py-6 rounded-lg text-center w-full max-w-3xl"
  >
    <h1
      class="text-3xl md:text-4xl font-extrabold mb-2 tracking-tight text-slate-50"
      style="text-shadow: 2px 2px 4px rgba(0,0,0,0.8);"
    >
      Dr. Hon. Antony Manyara, HSC.
    </h1>
    <p
      class="text-base md:text-lg mb-2 text-slate-50"
      style="text-shadow: 1px 1px 3px rgba(0,0,0,0.7);"
    >
      Visionary Youth Leader for Kenya’s Transformation
    </p>
    <a
      href="#about"
      class="inline-block bg-orange-500 hover:bg-orange-600 text-white font-semibold px-6 py-2 rounded-full shadow-md transform transition duration-300 hover:scale-105"
    >
      Learn More
    </a>
  </div>

  <!-- Jump to 60s on load & loop from 60s -->
  <script>
    const video = document.getElementById('bg-video');
    video.addEventListener('loadedmetadata', () => {
      video.currentTime = 60;
      video.play();
    });
    video.addEventListener('timeupdate', () => {
      if (video.currentTime >= video.duration) {
        video.currentTime = 60;
        video.play();
      }
    });
  </script>
</section>
